
# This file is automatically generated, you probably don't want to edit this

jmvMetaAnalysisRawOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvMetaAnalysisRawOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            labels = NULL,
            m1 = NULL,
            s1 = NULL,
            n1 = NULL,
            m2 = NULL,
            s2 = NULL,
            n2 = NULL,
            moderator = NULL,
            REorFE = "RE",
            g1label = NULL,
            g2label = NULL,
            conf.level = 95,
            report.cohens.d = FALSE,
            show.study.table = FALSE,
            explainDR = FALSE, ...) {

            super$initialize(
                package='esci',
                name='jmvMetaAnalysisRaw',
                requiresData=TRUE,
                ...)

            private$..labels <- jmvcore::OptionVariable$new(
                "labels",
                labels)
            private$..m1 <- jmvcore::OptionVariable$new(
                "m1",
                m1)
            private$..s1 <- jmvcore::OptionVariable$new(
                "s1",
                s1)
            private$..n1 <- jmvcore::OptionVariable$new(
                "n1",
                n1)
            private$..m2 <- jmvcore::OptionVariable$new(
                "m2",
                m2)
            private$..s2 <- jmvcore::OptionVariable$new(
                "s2",
                s2)
            private$..n2 <- jmvcore::OptionVariable$new(
                "n2",
                n2)
            private$..moderator <- jmvcore::OptionVariable$new(
                "moderator",
                moderator)
            private$..REorFE <- jmvcore::OptionList$new(
                "REorFE",
                REorFE,
                options=list(
                    "RE",
                    "FE"),
                default="RE")
            private$..g1label <- jmvcore::OptionString$new(
                "g1label",
                g1label)
            private$..g2label <- jmvcore::OptionString$new(
                "g2label",
                g2label)
            private$..conf.level <- jmvcore::OptionNumber$new(
                "conf.level",
                conf.level,
                min=50,
                max=99.9999,
                default=95)
            private$..report.cohens.d <- jmvcore::OptionBool$new(
                "report.cohens.d",
                report.cohens.d,
                default=FALSE)
            private$..show.study.table <- jmvcore::OptionBool$new(
                "show.study.table",
                show.study.table,
                default=FALSE)
            private$..explainDR <- jmvcore::OptionBool$new(
                "explainDR",
                explainDR,
                default=FALSE)

            self$.addOption(private$..labels)
            self$.addOption(private$..m1)
            self$.addOption(private$..s1)
            self$.addOption(private$..n1)
            self$.addOption(private$..m2)
            self$.addOption(private$..s2)
            self$.addOption(private$..n2)
            self$.addOption(private$..moderator)
            self$.addOption(private$..REorFE)
            self$.addOption(private$..g1label)
            self$.addOption(private$..g2label)
            self$.addOption(private$..conf.level)
            self$.addOption(private$..report.cohens.d)
            self$.addOption(private$..show.study.table)
            self$.addOption(private$..explainDR)
        }),
    active = list(
        labels = function() private$..labels$value,
        m1 = function() private$..m1$value,
        s1 = function() private$..s1$value,
        n1 = function() private$..n1$value,
        m2 = function() private$..m2$value,
        s2 = function() private$..s2$value,
        n2 = function() private$..n2$value,
        moderator = function() private$..moderator$value,
        REorFE = function() private$..REorFE$value,
        g1label = function() private$..g1label$value,
        g2label = function() private$..g2label$value,
        conf.level = function() private$..conf.level$value,
        report.cohens.d = function() private$..report.cohens.d$value,
        show.study.table = function() private$..show.study.table$value,
        explainDR = function() private$..explainDR$value),
    private = list(
        ..labels = NA,
        ..m1 = NA,
        ..s1 = NA,
        ..n1 = NA,
        ..m2 = NA,
        ..s2 = NA,
        ..n2 = NA,
        ..moderator = NA,
        ..REorFE = NA,
        ..g1label = NA,
        ..g2label = NA,
        ..conf.level = NA,
        ..report.cohens.d = NA,
        ..show.study.table = NA,
        ..explainDR = NA)
)

jmvMetaAnalysisRawResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]],
        result_table = function() private$.items[["result_table"]],
        study_table = function() private$.items[["study_table"]],
        forest_plot = function() private$.items[["forest_plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Meta-Analysis - Raw Scores")
            self$add(jmvcore::Html$new(
                options=options,
                name="text",
                title="Instructions/Errros",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="result_table",
                title="Meta-Analysis Results",
                visible=TRUE,
                rows=0,
                columns=list(
                    list(
                        `name`="label", 
                        `title`="Label", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="study_table",
                title="Table of Studies",
                visible=TRUE,
                rows=0,
                columns=list(
                    list(
                        `name`="label", 
                        `title`="Label", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="forest_plot",
                title="Forest Plot",
                visible=TRUE,
                width=500,
                height=800,
                renderFun=".plot"))}))

jmvMetaAnalysisRawBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvMetaAnalysisRawBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'esci',
                name = 'jmvMetaAnalysisRaw',
                version = c(1,0,0),
                options = options,
                results = jmvMetaAnalysisRawResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Meta-Analysis - Raw Scores
#'
#' 
#' @param data .
#' @param labels .
#' @param m1 .
#' @param s1 .
#' @param n1 .
#' @param m2 .
#' @param s2 .
#' @param n2 .
#' @param moderator .
#' @param REorFE .
#' @param g1label .
#' @param g2label .
#' @param conf.level .
#' @param report.cohens.d .
#' @param show.study.table .
#' @param explainDR .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$result_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$study_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$forest_plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$result_table$asDF}
#'
#' \code{as.data.frame(results$result_table)}
#'
#' @export
jmvMetaAnalysisRaw <- function(
    data,
    labels,
    m1,
    s1,
    n1,
    m2,
    s2,
    n2,
    moderator,
    REorFE = "RE",
    g1label,
    g2label,
    conf.level = 95,
    report.cohens.d = FALSE,
    show.study.table = FALSE,
    explainDR = FALSE) {

    if ( ! requireNamespace('jmvcore'))
        stop('jmvMetaAnalysisRaw requires jmvcore to be installed (restart may be required)')

    if ( ! missing(labels)) labels <- jmvcore::resolveQuo(jmvcore::enquo(labels))
    if ( ! missing(m1)) m1 <- jmvcore::resolveQuo(jmvcore::enquo(m1))
    if ( ! missing(s1)) s1 <- jmvcore::resolveQuo(jmvcore::enquo(s1))
    if ( ! missing(n1)) n1 <- jmvcore::resolveQuo(jmvcore::enquo(n1))
    if ( ! missing(m2)) m2 <- jmvcore::resolveQuo(jmvcore::enquo(m2))
    if ( ! missing(s2)) s2 <- jmvcore::resolveQuo(jmvcore::enquo(s2))
    if ( ! missing(n2)) n2 <- jmvcore::resolveQuo(jmvcore::enquo(n2))
    if ( ! missing(moderator)) moderator <- jmvcore::resolveQuo(jmvcore::enquo(moderator))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(labels), labels, NULL),
            `if`( ! missing(m1), m1, NULL),
            `if`( ! missing(s1), s1, NULL),
            `if`( ! missing(n1), n1, NULL),
            `if`( ! missing(m2), m2, NULL),
            `if`( ! missing(s2), s2, NULL),
            `if`( ! missing(n2), n2, NULL),
            `if`( ! missing(moderator), moderator, NULL))


    options <- jmvMetaAnalysisRawOptions$new(
        labels = labels,
        m1 = m1,
        s1 = s1,
        n1 = n1,
        m2 = m2,
        s2 = s2,
        n2 = n2,
        moderator = moderator,
        REorFE = REorFE,
        g1label = g1label,
        g2label = g2label,
        conf.level = conf.level,
        report.cohens.d = report.cohens.d,
        show.study.table = show.study.table,
        explainDR = explainDR)

    analysis <- jmvMetaAnalysisRawClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
