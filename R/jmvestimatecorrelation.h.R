
# This file is automatically generated, you probably don't want to edit this

jmvEstimateCorrelationOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvEstimateCorrelationOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            switch = "fromraw",
            dv = NULL,
            iv = NULL,
            show.line = FALSE,
            show.meanCI = FALSE,
            show.PI = FALSE,
            predictx = "none",
            r = NULL,
            n = NULL,
            conf.level = 95,
            ylab = NULL,
            xlab = NULL,
            size = 3,
            ymin = "auto",
            ymax = "auto",
            xmin = "auto",
            xmax = "auto", ...) {

            super$initialize(
                package='esci',
                name='jmvEstimateCorrelation',
                requiresData=TRUE,
                ...)

            private$..switch <- jmvcore::OptionList$new(
                "switch",
                switch,
                default="fromraw",
                options=list(
                    "fromraw",
                    "fromsummary"))
            private$..dv <- jmvcore::OptionVariable$new(
                "dv",
                dv,
                permitted=list(
                    "numeric"))
            private$..iv <- jmvcore::OptionVariable$new(
                "iv",
                iv,
                permitted=list(
                    "numeric"))
            private$..show.line <- jmvcore::OptionBool$new(
                "show.line",
                show.line,
                default=FALSE)
            private$..show.meanCI <- jmvcore::OptionBool$new(
                "show.meanCI",
                show.meanCI,
                default=FALSE)
            private$..show.PI <- jmvcore::OptionBool$new(
                "show.PI",
                show.PI,
                default=FALSE)
            private$..predictx <- jmvcore::OptionString$new(
                "predictx",
                predictx,
                default="none")
            private$..r <- jmvcore::OptionNumber$new(
                "r",
                r,
                min=0,
                max=1)
            private$..n <- jmvcore::OptionNumber$new(
                "n",
                n)
            private$..conf.level <- jmvcore::OptionNumber$new(
                "conf.level",
                conf.level,
                min=50,
                max=99.9999,
                default=95)
            private$..ylab <- jmvcore::OptionString$new(
                "ylab",
                ylab)
            private$..xlab <- jmvcore::OptionString$new(
                "xlab",
                xlab)
            private$..size <- jmvcore::OptionNumber$new(
                "size",
                size,
                default=3)
            private$..ymin <- jmvcore::OptionString$new(
                "ymin",
                ymin,
                default="auto")
            private$..ymax <- jmvcore::OptionString$new(
                "ymax",
                ymax,
                default="auto")
            private$..xmin <- jmvcore::OptionString$new(
                "xmin",
                xmin,
                default="auto")
            private$..xmax <- jmvcore::OptionString$new(
                "xmax",
                xmax,
                default="auto")

            self$.addOption(private$..switch)
            self$.addOption(private$..dv)
            self$.addOption(private$..iv)
            self$.addOption(private$..show.line)
            self$.addOption(private$..show.meanCI)
            self$.addOption(private$..show.PI)
            self$.addOption(private$..predictx)
            self$.addOption(private$..r)
            self$.addOption(private$..n)
            self$.addOption(private$..conf.level)
            self$.addOption(private$..ylab)
            self$.addOption(private$..xlab)
            self$.addOption(private$..size)
            self$.addOption(private$..ymin)
            self$.addOption(private$..ymax)
            self$.addOption(private$..xmin)
            self$.addOption(private$..xmax)
        }),
    active = list(
        switch = function() private$..switch$value,
        dv = function() private$..dv$value,
        iv = function() private$..iv$value,
        show.line = function() private$..show.line$value,
        show.meanCI = function() private$..show.meanCI$value,
        show.PI = function() private$..show.PI$value,
        predictx = function() private$..predictx$value,
        r = function() private$..r$value,
        n = function() private$..n$value,
        conf.level = function() private$..conf.level$value,
        ylab = function() private$..ylab$value,
        xlab = function() private$..xlab$value,
        size = function() private$..size$value,
        ymin = function() private$..ymin$value,
        ymax = function() private$..ymax$value,
        xmin = function() private$..xmin$value,
        xmax = function() private$..xmax$value),
    private = list(
        ..switch = NA,
        ..dv = NA,
        ..iv = NA,
        ..show.line = NA,
        ..show.meanCI = NA,
        ..show.PI = NA,
        ..predictx = NA,
        ..r = NA,
        ..n = NA,
        ..conf.level = NA,
        ..ylab = NA,
        ..xlab = NA,
        ..size = NA,
        ..ymin = NA,
        ..ymax = NA,
        ..xmin = NA,
        ..xmax = NA)
)

jmvEstimateCorrelationResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]],
        summary_table = function() private$.items[["summary_table"]],
        scatter_plot = function() private$.items[["scatter_plot"]],
        correlation_plot = function() private$.items[["correlation_plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Estimate Correlation")
            self$add(jmvcore::Html$new(
                options=options,
                name="text",
                title="Instructions/Errors",
                visible=FALSE))
            self$add(jmvcore::Table$new(
                options=options,
                name="summary_table",
                title="Estimated Correlation",
                visible=TRUE,
                rows=1,
                columns=list(
                    list(
                        `name`="var", 
                        `title`="Relationship", 
                        `type`="text"),
                    list(
                        `name`="r", 
                        `title`="Pearson's r", 
                        `type`="number"),
                    list(
                        `name`="ci.low", 
                        `title`="Lower", 
                        `type`="number"),
                    list(
                        `name`="ci.high", 
                        `title`="Higher", 
                        `type`="number"),
                    list(
                        `name`="n", 
                        `title`="N", 
                        `type`="Integer"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="scatter_plot",
                title="Scatter Plot",
                visible="(switch:fromraw)",
                width=400,
                height=400,
                requiresData=TRUE,
                renderFun=".scatter_plot"))
            self$add(jmvcore::Image$new(
                options=options,
                name="correlation_plot",
                title="Correlation Plot",
                visible=TRUE,
                width=300,
                height=530,
                requiresData=TRUE,
                renderFun=".correlation_plot"))}))

jmvEstimateCorrelationBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvEstimateCorrelationBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'esci',
                name = 'jmvEstimateCorrelation',
                version = c(1,0,0),
                options = options,
                results = jmvEstimateCorrelationResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Estimate Correlation
#'
#' 
#' @param switch .
#' @param data .
#' @param dv .
#' @param iv .
#' @param show.line .
#' @param show.meanCI .
#' @param show.PI .
#' @param predictx .
#' @param r .
#' @param n .
#' @param conf.level .
#' @param ylab .
#' @param xlab .
#' @param size .
#' @param ymin .
#' @param ymax .
#' @param xmin .
#' @param xmax .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$summary_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$scatter_plot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$correlation_plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$summary_table$asDF}
#'
#' \code{as.data.frame(results$summary_table)}
#'
#' @export
jmvEstimateCorrelation <- function(
    switch = "fromraw",
    data,
    dv,
    iv,
    show.line = FALSE,
    show.meanCI = FALSE,
    show.PI = FALSE,
    predictx = "none",
    r,
    n,
    conf.level = 95,
    ylab,
    xlab,
    size = 3,
    ymin = "auto",
    ymax = "auto",
    xmin = "auto",
    xmax = "auto") {

    if ( ! requireNamespace('jmvcore'))
        stop('jmvEstimateCorrelation requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dv)) dv <- jmvcore::resolveQuo(jmvcore::enquo(dv))
    if ( ! missing(iv)) iv <- jmvcore::resolveQuo(jmvcore::enquo(iv))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dv), dv, NULL),
            `if`( ! missing(iv), iv, NULL))


    options <- jmvEstimateCorrelationOptions$new(
        switch = switch,
        dv = dv,
        iv = iv,
        show.line = show.line,
        show.meanCI = show.meanCI,
        show.PI = show.PI,
        predictx = predictx,
        r = r,
        n = n,
        conf.level = conf.level,
        ylab = ylab,
        xlab = xlab,
        size = size,
        ymin = ymin,
        ymax = ymax,
        xmin = xmin,
        xmax = xmax)

    analysis <- jmvEstimateCorrelationClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
