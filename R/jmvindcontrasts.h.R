
# This file is automatically generated, you probably don't want to edit this

jmvIndContrastsOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvIndContrastsOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            switch = "fromraw",
            dep = NULL,
            group = NULL,
            labels = NULL,
            means = NULL,
            sds = NULL,
            ns = NULL,
            ref_labels = NULL,
            comparison_labels = NULL,
            conf.level = 95,
            show.mean.error = TRUE,
            show.raw.data = TRUE,
            ylab = NULL,
            contrast.colors = "blue, green", ...) {

            super$initialize(
                package='esci',
                name='jmvIndContrasts',
                requiresData=TRUE,
                ...)

            private$..switch <- jmvcore::OptionList$new(
                "switch",
                switch,
                default="fromraw",
                options=list(
                    "fromraw",
                    "fromsummary"))
            private$..dep <- jmvcore::OptionVariable$new(
                "dep",
                dep)
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group)
            private$..labels <- jmvcore::OptionVariable$new(
                "labels",
                labels)
            private$..means <- jmvcore::OptionVariable$new(
                "means",
                means)
            private$..sds <- jmvcore::OptionVariable$new(
                "sds",
                sds)
            private$..ns <- jmvcore::OptionVariable$new(
                "ns",
                ns)
            private$..ref_labels <- jmvcore::OptionString$new(
                "ref_labels",
                ref_labels)
            private$..comparison_labels <- jmvcore::OptionString$new(
                "comparison_labels",
                comparison_labels)
            private$..conf.level <- jmvcore::OptionNumber$new(
                "conf.level",
                conf.level,
                min=50,
                max=99.9999,
                default=95)
            private$..show.mean.error <- jmvcore::OptionBool$new(
                "show.mean.error",
                show.mean.error,
                default=TRUE)
            private$..show.raw.data <- jmvcore::OptionBool$new(
                "show.raw.data",
                show.raw.data,
                default=TRUE)
            private$..ylab <- jmvcore::OptionString$new(
                "ylab",
                ylab)
            private$..contrast.colors <- jmvcore::OptionString$new(
                "contrast.colors",
                contrast.colors,
                default="blue, green")

            self$.addOption(private$..switch)
            self$.addOption(private$..dep)
            self$.addOption(private$..group)
            self$.addOption(private$..labels)
            self$.addOption(private$..means)
            self$.addOption(private$..sds)
            self$.addOption(private$..ns)
            self$.addOption(private$..ref_labels)
            self$.addOption(private$..comparison_labels)
            self$.addOption(private$..conf.level)
            self$.addOption(private$..show.mean.error)
            self$.addOption(private$..show.raw.data)
            self$.addOption(private$..ylab)
            self$.addOption(private$..contrast.colors)
        }),
    active = list(
        switch = function() private$..switch$value,
        dep = function() private$..dep$value,
        group = function() private$..group$value,
        labels = function() private$..labels$value,
        means = function() private$..means$value,
        sds = function() private$..sds$value,
        ns = function() private$..ns$value,
        ref_labels = function() private$..ref_labels$value,
        comparison_labels = function() private$..comparison_labels$value,
        conf.level = function() private$..conf.level$value,
        show.mean.error = function() private$..show.mean.error$value,
        show.raw.data = function() private$..show.raw.data$value,
        ylab = function() private$..ylab$value,
        contrast.colors = function() private$..contrast.colors$value),
    private = list(
        ..switch = NA,
        ..dep = NA,
        ..group = NA,
        ..labels = NA,
        ..means = NA,
        ..sds = NA,
        ..ns = NA,
        ..ref_labels = NA,
        ..comparison_labels = NA,
        ..conf.level = NA,
        ..show.mean.error = NA,
        ..show.raw.data = NA,
        ..ylab = NA,
        ..contrast.colors = NA)
)

jmvIndContrastsResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]],
        means_table = function() private$.items[["means_table"]],
        contrast_table = function() private$.items[["contrast_table"]],
        contrast_plot = function() private$.items[["contrast_plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Estimate Ind. Groups Contrasts")
            self$add(jmvcore::Html$new(
                options=options,
                name="text",
                title="Instructions/Errors",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="means_table",
                title="Descriptive Statistics",
                visible=TRUE,
                rows=1,
                columns=list(
                    list(
                        `name`="label", 
                        `title`="Label", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="contrast_table",
                title="Contrasts",
                visible=TRUE,
                rows=1,
                columns=list(
                    list(
                        `name`="label", 
                        `title`="Label", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="contrast_plot",
                title="Contrast Plot",
                visible=TRUE,
                width=900,
                height=550,
                renderFun=".plot"))}))

jmvIndContrastsBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvIndContrastsBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'esci',
                name = 'jmvIndContrasts',
                version = c(1,0,0),
                options = options,
                results = jmvIndContrastsResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Estimate Ind. Groups Contrasts
#'
#' 
#' @param switch .
#' @param data .
#' @param dep .
#' @param group .
#' @param labels .
#' @param means .
#' @param sds .
#' @param ns .
#' @param ref_labels .
#' @param comparison_labels .
#' @param conf.level .
#' @param show.mean.error .
#' @param show.raw.data .
#' @param ylab .
#' @param contrast.colors .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$means_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$contrast_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$contrast_plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$means_table$asDF}
#'
#' \code{as.data.frame(results$means_table)}
#'
#' @export
jmvIndContrasts <- function(
    switch = "fromraw",
    data,
    dep,
    group,
    labels,
    means,
    sds,
    ns,
    ref_labels,
    comparison_labels,
    conf.level = 95,
    show.mean.error = TRUE,
    show.raw.data = TRUE,
    ylab,
    contrast.colors = "blue, green") {

    if ( ! requireNamespace('jmvcore'))
        stop('jmvIndContrasts requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dep)) dep <- jmvcore::resolveQuo(jmvcore::enquo(dep))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if ( ! missing(labels)) labels <- jmvcore::resolveQuo(jmvcore::enquo(labels))
    if ( ! missing(means)) means <- jmvcore::resolveQuo(jmvcore::enquo(means))
    if ( ! missing(sds)) sds <- jmvcore::resolveQuo(jmvcore::enquo(sds))
    if ( ! missing(ns)) ns <- jmvcore::resolveQuo(jmvcore::enquo(ns))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dep), dep, NULL),
            `if`( ! missing(group), group, NULL),
            `if`( ! missing(labels), labels, NULL),
            `if`( ! missing(means), means, NULL),
            `if`( ! missing(sds), sds, NULL),
            `if`( ! missing(ns), ns, NULL))


    options <- jmvIndContrastsOptions$new(
        switch = switch,
        dep = dep,
        group = group,
        labels = labels,
        means = means,
        sds = sds,
        ns = ns,
        ref_labels = ref_labels,
        comparison_labels = comparison_labels,
        conf.level = conf.level,
        show.mean.error = show.mean.error,
        show.raw.data = show.raw.data,
        ylab = ylab,
        contrast.colors = contrast.colors)

    analysis <- jmvIndContrastsClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
