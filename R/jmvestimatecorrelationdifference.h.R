
# This file is automatically generated, you probably don't want to edit this

jmvEstimateCorrelationDifferenceOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvEstimateCorrelationDifferenceOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            switch = "fromraw",
            dv = NULL,
            iv = NULL,
            group = NULL,
            r1 = NULL,
            n1 = NULL,
            r2 = NULL,
            n2 = NULL,
            grouplabel1 = "Group 1",
            grouplabel2 = "Group 2",
            varlabel1 = "Variable 1",
            varlabel2 = "Variable 2",
            conf.level = 95, ...) {

            super$initialize(
                package='esci',
                name='jmvEstimateCorrelationDifference',
                requiresData=TRUE,
                ...)

            private$..switch <- jmvcore::OptionList$new(
                "switch",
                switch,
                default="fromraw",
                options=list(
                    "fromraw",
                    "fromsummary"))
            private$..dv <- jmvcore::OptionVariable$new(
                "dv",
                dv)
            private$..iv <- jmvcore::OptionVariable$new(
                "iv",
                iv)
            private$..group <- jmvcore::OptionVariable$new(
                "group",
                group)
            private$..r1 <- jmvcore::OptionNumber$new(
                "r1",
                r1)
            private$..n1 <- jmvcore::OptionInteger$new(
                "n1",
                n1)
            private$..r2 <- jmvcore::OptionNumber$new(
                "r2",
                r2)
            private$..n2 <- jmvcore::OptionInteger$new(
                "n2",
                n2)
            private$..grouplabel1 <- jmvcore::OptionString$new(
                "grouplabel1",
                grouplabel1,
                default="Group 1")
            private$..grouplabel2 <- jmvcore::OptionString$new(
                "grouplabel2",
                grouplabel2,
                default="Group 2")
            private$..varlabel1 <- jmvcore::OptionString$new(
                "varlabel1",
                varlabel1,
                default="Variable 1")
            private$..varlabel2 <- jmvcore::OptionString$new(
                "varlabel2",
                varlabel2,
                default="Variable 2")
            private$..conf.level <- jmvcore::OptionNumber$new(
                "conf.level",
                conf.level,
                min=50,
                max=99.9999,
                default=95)

            self$.addOption(private$..switch)
            self$.addOption(private$..dv)
            self$.addOption(private$..iv)
            self$.addOption(private$..group)
            self$.addOption(private$..r1)
            self$.addOption(private$..n1)
            self$.addOption(private$..r2)
            self$.addOption(private$..n2)
            self$.addOption(private$..grouplabel1)
            self$.addOption(private$..grouplabel2)
            self$.addOption(private$..varlabel1)
            self$.addOption(private$..varlabel2)
            self$.addOption(private$..conf.level)
        }),
    active = list(
        switch = function() private$..switch$value,
        dv = function() private$..dv$value,
        iv = function() private$..iv$value,
        group = function() private$..group$value,
        r1 = function() private$..r1$value,
        n1 = function() private$..n1$value,
        r2 = function() private$..r2$value,
        n2 = function() private$..n2$value,
        grouplabel1 = function() private$..grouplabel1$value,
        grouplabel2 = function() private$..grouplabel2$value,
        varlabel1 = function() private$..varlabel1$value,
        varlabel2 = function() private$..varlabel2$value,
        conf.level = function() private$..conf.level$value),
    private = list(
        ..switch = NA,
        ..dv = NA,
        ..iv = NA,
        ..group = NA,
        ..r1 = NA,
        ..n1 = NA,
        ..r2 = NA,
        ..n2 = NA,
        ..grouplabel1 = NA,
        ..grouplabel2 = NA,
        ..varlabel1 = NA,
        ..varlabel2 = NA,
        ..conf.level = NA)
)

jmvEstimateCorrelationDifferenceResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]],
        result_table = function() private$.items[["result_table"]],
        allrs = function() private$.items[["allrs"]],
        scatter_plot = function() private$.items[["scatter_plot"]],
        correlation_plot = function() private$.items[["correlation_plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Estimate Correlation Difference")
            self$add(jmvcore::Html$new(
                options=options,
                name="text",
                title="Instructions/Errros",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="result_table",
                title="Difference between correlations",
                rows=0,
                columns=list(
                    list(
                        `name`="variables", 
                        `title`="Variables", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="allrs",
                title="All group correlations",
                rows=0,
                columns=list(
                    list(
                        `name`="group", 
                        `title`="Group", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="scatter_plot",
                title="Scatter Plot",
                width=600,
                height=600,
                renderFun=".plotSP"))
            self$add(jmvcore::Image$new(
                options=options,
                name="correlation_plot",
                title="Correlation Plot",
                width=500,
                height=600,
                renderFun=".plotCP"))}))

jmvEstimateCorrelationDifferenceBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvEstimateCorrelationDifferenceBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'esci',
                name = 'jmvEstimateCorrelationDifference',
                version = c(1,0,0),
                options = options,
                results = jmvEstimateCorrelationDifferenceResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Estimate Correlation Difference
#'
#' 
#' @param switch .
#' @param data .
#' @param dv .
#' @param iv .
#' @param group .
#' @param r1 .
#' @param n1 .
#' @param r2 .
#' @param n2 .
#' @param grouplabel1 .
#' @param grouplabel2 .
#' @param varlabel1 .
#' @param varlabel2 .
#' @param conf.level .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a html \cr
#'   \code{results$result_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$allrs} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$scatter_plot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$correlation_plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$result_table$asDF}
#'
#' \code{as.data.frame(results$result_table)}
#'
#' @export
jmvEstimateCorrelationDifference <- function(
    switch = "fromraw",
    data,
    dv,
    iv,
    group,
    r1,
    n1,
    r2,
    n2,
    grouplabel1 = "Group 1",
    grouplabel2 = "Group 2",
    varlabel1 = "Variable 1",
    varlabel2 = "Variable 2",
    conf.level = 95) {

    if ( ! requireNamespace('jmvcore'))
        stop('jmvEstimateCorrelationDifference requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dv)) dv <- jmvcore::resolveQuo(jmvcore::enquo(dv))
    if ( ! missing(iv)) iv <- jmvcore::resolveQuo(jmvcore::enquo(iv))
    if ( ! missing(group)) group <- jmvcore::resolveQuo(jmvcore::enquo(group))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dv), dv, NULL),
            `if`( ! missing(iv), iv, NULL),
            `if`( ! missing(group), group, NULL))


    options <- jmvEstimateCorrelationDifferenceOptions$new(
        switch = switch,
        dv = dv,
        iv = iv,
        group = group,
        r1 = r1,
        n1 = n1,
        r2 = r2,
        n2 = n2,
        grouplabel1 = grouplabel1,
        grouplabel2 = grouplabel2,
        varlabel1 = varlabel1,
        varlabel2 = varlabel2,
        conf.level = conf.level)

    analysis <- jmvEstimateCorrelationDifferenceClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
