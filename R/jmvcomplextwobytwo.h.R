
# This file is automatically generated, you probably don't want to edit this

jmvComplexTwoByTwoOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvComplexTwoByTwoOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            dep = NULL,
            group1 = NULL,
            group2 = NULL,
            MEorInt = "fInt",
            conf.level = 0.95,
            show.mean.error = FALSE,
            show.raw.data = FALSE,
            ylab = NULL, ...) {

            super$initialize(
                package='esci',
                name='jmvComplexTwoByTwo',
                requiresData=TRUE,
                ...)

            private$..dep <- jmvcore::OptionVariable$new(
                "dep",
                dep)
            private$..group1 <- jmvcore::OptionVariable$new(
                "group1",
                group1)
            private$..group2 <- jmvcore::OptionVariable$new(
                "group2",
                group2)
            private$..MEorInt <- jmvcore::OptionList$new(
                "MEorInt",
                MEorInt,
                options=list(
                    "fME",
                    "fInt"),
                default="fInt")
            private$..conf.level <- jmvcore::OptionNumber$new(
                "conf.level",
                conf.level,
                min=0.5,
                max=0.999999,
                default=0.95)
            private$..show.mean.error <- jmvcore::OptionBool$new(
                "show.mean.error",
                show.mean.error,
                default=FALSE)
            private$..show.raw.data <- jmvcore::OptionBool$new(
                "show.raw.data",
                show.raw.data,
                default=FALSE)
            private$..ylab <- jmvcore::OptionString$new(
                "ylab",
                ylab)

            self$.addOption(private$..dep)
            self$.addOption(private$..group1)
            self$.addOption(private$..group2)
            self$.addOption(private$..MEorInt)
            self$.addOption(private$..conf.level)
            self$.addOption(private$..show.mean.error)
            self$.addOption(private$..show.raw.data)
            self$.addOption(private$..ylab)
        }),
    active = list(
        dep = function() private$..dep$value,
        group1 = function() private$..group1$value,
        group2 = function() private$..group2$value,
        MEorInt = function() private$..MEorInt$value,
        conf.level = function() private$..conf.level$value,
        show.mean.error = function() private$..show.mean.error$value,
        show.raw.data = function() private$..show.raw.data$value,
        ylab = function() private$..ylab$value),
    private = list(
        ..dep = NA,
        ..group1 = NA,
        ..group2 = NA,
        ..MEorInt = NA,
        ..conf.level = NA,
        ..show.mean.error = NA,
        ..show.raw.data = NA,
        ..ylab = NA)
)

jmvComplexTwoByTwoResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]],
        means_table = function() private$.items[["means_table"]],
        contrast_table = function() private$.items[["contrast_table"]],
        ME1_plot = function() private$.items[["ME1_plot"]],
        ME2_plot = function() private$.items[["ME2_plot"]],
        Int_plot = function() private$.items[["Int_plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Estimate Ind. 2x2")
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Instructions/Errors",
                visible=TRUE))
            self$add(jmvcore::Table$new(
                options=options,
                name="means_table",
                title="Descriptive Statistics",
                visible=TRUE,
                rows=0,
                columns=list(
                    list(
                        `name`="label", 
                        `title`="Label", 
                        `type`="text"))))
            self$add(jmvcore::Table$new(
                options=options,
                name="contrast_table",
                title="Contrasts",
                visible=TRUE,
                rows=0,
                columns=list(
                    list(
                        `name`="label", 
                        `title`="Label", 
                        `type`="text"))))
            self$add(jmvcore::Image$new(
                options=options,
                name="ME1_plot",
                title="Main Effect 1 Plot",
                visible=FALSE,
                width=600,
                height=350,
                renderFun=".ME1plot"))
            self$add(jmvcore::Image$new(
                options=options,
                name="ME2_plot",
                title="Main Effect 2 Plot",
                visible=FALSE,
                width=600,
                height=350,
                renderFun=".ME2plot"))
            self$add(jmvcore::Image$new(
                options=options,
                name="Int_plot",
                title="Interaction Plot",
                visible=FALSE,
                width=600,
                height=350,
                renderFun=".Intplot"))}))

jmvComplexTwoByTwoBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "jmvComplexTwoByTwoBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'esci',
                name = 'jmvComplexTwoByTwo',
                version = c(1,0,0),
                options = options,
                results = jmvComplexTwoByTwoResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Estimate Ind. 2x2
#'
#' 
#' @param data .
#' @param dep .
#' @param group1 .
#' @param group2 .
#' @param MEorInt .
#' @param conf.level .
#' @param show.mean.error .
#' @param show.raw.data .
#' @param ylab .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$means_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$contrast_table} \tab \tab \tab \tab \tab a table \cr
#'   \code{results$ME1_plot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$ME2_plot} \tab \tab \tab \tab \tab an image \cr
#'   \code{results$Int_plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' Tables can be converted to data frames with \code{asDF} or \code{\link{as.data.frame}}. For example:
#'
#' \code{results$means_table$asDF}
#'
#' \code{as.data.frame(results$means_table)}
#'
#' @export
jmvComplexTwoByTwo <- function(
    data,
    dep,
    group1,
    group2,
    MEorInt = "fInt",
    conf.level = 0.95,
    show.mean.error = FALSE,
    show.raw.data = FALSE,
    ylab) {

    if ( ! requireNamespace('jmvcore'))
        stop('jmvComplexTwoByTwo requires jmvcore to be installed (restart may be required)')

    if ( ! missing(dep)) dep <- jmvcore::resolveQuo(jmvcore::enquo(dep))
    if ( ! missing(group1)) group1 <- jmvcore::resolveQuo(jmvcore::enquo(group1))
    if ( ! missing(group2)) group2 <- jmvcore::resolveQuo(jmvcore::enquo(group2))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(dep), dep, NULL),
            `if`( ! missing(group1), group1, NULL),
            `if`( ! missing(group2), group2, NULL))


    options <- jmvComplexTwoByTwoOptions$new(
        dep = dep,
        group1 = group1,
        group2 = group2,
        MEorInt = MEorInt,
        conf.level = conf.level,
        show.mean.error = show.mean.error,
        show.raw.data = show.raw.data,
        ylab = ylab)

    analysis <- jmvComplexTwoByTwoClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
